repo: "https://github.com/svc-design/gitops.git"
branch: main        # ✅ 添加这一项，指定使用的分支
interval: 10

playbook:
  - sync/config.yaml

# 可选：覆盖默认同步目录和状态目录
# sync_dir: /var/lib/xconfig-agent/repo
# status_dir: /var/lib/xconfig-agent/status
# node_id: edge-node-01
